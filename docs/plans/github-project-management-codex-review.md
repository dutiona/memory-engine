[Codex Review] [BLOCKER] Task 3/10 GraphQL calls use `-f q=...` instead of `-f query=...`, so `gh api graphql` will not receive a GraphQL query. The `gh api` help says GraphQL requests treat fields other than `query` and `operationName` as variables; therefore `owner_id`, `project_item_add`, `subissue_link`, `create_or_get`, `field_id_by_name`, `set_status_opts`, `create_select`, and the field query in Task 10 are structurally wrong as written (`docs/plans/2026-05-31-github-project-management.md:180`, `:182`, `:184`, `:416`, `:418`-`:420`, `:429`). The plan needs a global correction to use `query` for every GraphQL document before these scripts are executable.

[Codex Review] [BLOCKER] Task 10 still contains an unimplemented placeholder for the runtime state cache that Task 11 depends on. The plan promises `state.json` will contain field IDs and option-name to option-ID maps (`docs/plans/2026-05-31-github-project-management.md:70`, `:411`), but the script only writes Main fields to `/tmp/me_main_fields.json` and leaves “repeat per project; merge into state.json” as a comment (`:428`-`:430`). Task 11 then requires `state.json` for `updateProjectV2ItemFieldValue` (`:440`). This breaks field backfill, Priority/Phase setting, idempotency verification, and fresh-checkout regeneration.

[Codex Review] [BLOCKER] Task 10 passes `singleSelectOptions` as `-F o="$(opts_json ...)"`, but `gh api -F` does not parse arbitrary JSON arrays into GraphQL variables; it only performs scalar conversion, file reads, and nested/array syntax via repeated `key[]=...` fields. As written, `$o` is likely sent as a string, not `[ProjectV2SingleSelectFieldOptionInput!]`, so both `updateProjectV2Field` and `createProjectV2Field` calls will fail type validation (`docs/plans/2026-05-31-github-project-management.md:417`-`:420`). Use an explicit JSON request body via `--input -` or construct proper nested variables.

[Codex Review] [HIGH] Task 10’s Status reconciliation can clear existing project field values on rerun because it overwrites `singleSelectOptions` without preserving existing option IDs. GitHub’s ProjectV2 option input includes `id` specifically to preserve option identity during updates; the docs say supplied values overwrite existing options and existing configuration should be fetched for partial updates. The plan’s `opts_json` always emits only `{name,color,description}` (`docs/plans/2026-05-31-github-project-management.md:417`, `:419`), so a rerun after items have Status values can recreate options and drop current Status assignments. The reconciler must query current Status options and include each existing option `id` when names match.

[Codex Review] [HIGH] `project_item_add` is not idempotent once a project has more than 100 items because it only queries `items(first:100)` before deciding whether to add (`docs/plans/2026-05-31-github-project-management.md:182`). The Main project is expected to contain at least 97 open issues plus 8 newly-created epics, and future growth will exceed 100 (`:440`-`:441`). Re-running after the existing item falls outside the first page can attempt to add duplicates or hit API errors. The helper needs pagination or a direct content lookup strategy.

[Codex Review] [HIGH] Task 11 is too underspecified for the most fragile Projects-v2 mutation. It says to set fields with `updateProjectV2ItemFieldValue` using `state.json`, but gives no concrete mutation shape, no lookup from issue number to project item ID, no handling for unset Phase/Priority, and no validation that every selected option name exists in the cache (`docs/plans/2026-05-31-github-project-management.md:436`-`:441`). Given the plan’s goal of idempotent Bash/GraphQL reconcilers, this is a correctness gap, not just implementation detail.

[Codex Review] [HIGH] The workflow uses `actions/add-to-project@v1` while holding a fine-grained PAT with Projects write permission, and the plan explicitly leaves SHA pinning as a follow-up (`docs/plans/2026-05-31-github-project-management.md:458`, `:471`, `:478`, `:574`). That violates the security criterion for supply-chain hardening. Pin the action to a reviewed full-length commit SHA in `12-render-workflow.sh` and document the update process.

[Codex Review] [MEDIUM] The generated issue-map rules are ordered after Task 5’s label renames, but they still only recognize the old label names (`bug`, `security`, `testing`, `documentation`, `refactoring`, `research`) and do not recognize canonical labels such as `type:bug`, `type:docs`, `type:research`, or the bootstrapped `type:plan` label (`docs/plans/2026-05-31-github-project-management.md:214`-`:221`, `:237`-`:263`). The new plan issue is titled `plan(build): ...`, not `Plan:` (`:163`, `:253`-`:255`), so it will be marked `AMBIGUOUS` despite the comment that Task 6 applies its area. Human review can repair it, but the generator no longer “pre-fills what is machine-derivable” as promised (`:114`).

[Codex Review] [MEDIUM] `restore.sh` is described as removing sub-issue links “recorded in state.json” (`docs/plans/2026-05-31-github-project-management.md:194`), but no later task records the links created by `07-epics.sh` into `state.json`; Task 10’s state cache is project/field-oriented, and Task 12 only says to link/verify sub-issues (`:428`-`:430`, `:447`-`:450`). Rollback of epic relationships is therefore not actually specified.

REVIEW COMPLETE
