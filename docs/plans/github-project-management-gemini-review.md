[Gemini Review] [BLOCKER] **Sequencing/Logic mismatch in `gen-issue-map.sh` (Task 6):** The generator script's `case` logic in Task 6 expects old label names (e.g., `bug`, `security`, `testing`, `documentation`) to derive the new `type:*` categories. However, Task 6 is executed after Task 5, which has already renamed these labels to `type:bug`, `type:docs`, etc. Consequently, the pattern matches (e.g., `*,bug,*)`) will fail to find any matches in the refreshed `open-issues.json`, causing the script to default to `AMBIGUOUS` for nearly all migrated issues.

[Gemini Review] [BLOCKER] **Multi-type Label Accumulation in `02-migrate-issues.sh` (Task 6):** The migration script only explicitly removes `type:enhancement` when the target type is different. It fails to remove other renamed type labels (e.g., `type:docs`, `type:refactor`, `type:research`, `type:test`, `type:security`). If an issue previously labelled `documentation` (renamed to `type:docs` in Task 5) is mapped to `type:bug`, the script will add `type:bug` but leave `type:docs` on the issue. This results in issues with multiple type labels, which violates the `INV-VERIFY` gate logic in `03-verify.sh`.

[Gemini Review] [HIGH] **Insufficient PAT Scopes (Task 13):** The fine-grained PAT described in Task 13 specifies scopes "Issues/PR/Metadata=read". This is insufficient for the migration scripts and the auto-add workflow to perform their tasks. The PAT requires **Write** access to `Issues` and `Pull Requests` to apply labels (`02-migrate-issues.sh`) and to `Projects` to manage items.

[Gemini Review] [MEDIUM] **Idempotency Hole in Issue Migration (Task 6):** The `02-migrate-issues.sh` script is not fully idempotent regarding the desired state. It only removes a subset of labels (`design`, `performance`, `quality`, and `type:enhancement`). If an issue's mapping is changed and the script is re-run, it will accumulate new `type:*` and `area:*` labels without removing the ones from the previous run. To ensure idempotency (convergence to desired state), the script should remove all existing `type:*` and `area:*` labels before adding the ones specified in the manifest.

[Gemini Review] [MEDIUM] **SHA-pinning for GitHub Actions (Task 13):** The plan acknowledges the security risk of using version tags for GitHub Actions and suggests SHA-pinning as a "hardening follow-up" in a comment. However, the implementation in Task 13 Step 3 still uses `actions/add-to-project@v1`. Given the "Security First" requirement, the action should be pinned to a specific commit SHA in the implemented workflow.

[Gemini Review] [LOW] **GraphQL Mutation `addSubIssue` Parameter (Task 3/12):** The `subissue_link` helper uses `replaceParent: true` in the `addSubIssue` mutation. While the plan states this was "empirically verified," this parameter is not documented in the standard GitHub GraphQL public API for `AddSubIssueInput`. If this is a preview feature or a custom extension, it should be explicitly noted; otherwise, there is a risk of the mutation failing in different environments or with different `gh` versions.

[Gemini Review] [LOW] **Redundant Label Removal (Task 6):** In `02-migrate-issues.sh`, the removal of `type:enhancement` is gated by `[ "$type" != "type:enhancement" ]`. While `gh issue edit` handles removing a non-existent label gracefully, including it in the `rm` array unconditionally (along with other type labels) would simplify the script and ensure better cleanup.

REVIEW COMPLETE.
