[project]
name = "memory-engine-docs"
version = "0.1.0"
description = "Documentation build dependencies for memory-engine"
requires-python = ">=3.12"
dependencies = [
    "sphinx>=8.0",
    "myst-parser>=4.0",
    "sphinx-rtd-theme>=3.0",
    "sphinx-copybutton>=0.5",
    "sphinxcontrib-mermaid>=1.0",
]
